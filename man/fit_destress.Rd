% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{fit_destress}
\alias{fit_destress}
\title{Fit DStressR with staged statistical options}
\usage{
fit_destress(
  assay,
  technical = NULL,
  empirical_bayes = TRUE,
  empty_vector_reporter = NULL,
  background_rank = 0,
  background_rank_max = 5,
  background_rank_permutations = 100,
  background_rank_threshold = 0.99,
  background_rank_seed = NULL,
  background_impute = c("column_mean", "global_mean", "zero"),
  normalization = NULL,
  testing = NULL,
  aggregation = NULL,
  adjustment = NULL,
  interaction = FALSE,
  preset = NULL,
  ...
)
}
\arguments{
\item{assay}{A \code{destress_assay} produced by \code{\link[=prepare_assay]{prepare_assay()}} or a raw assay
data frame for \code{normalization = "linear_model"}, or a long expression
table for the compatibility presets.}

\item{technical}{Character vector of batch, plate, replicate, or other
technical-factor columns to include.}

\item{empirical_bayes}{If \code{TRUE}, moderates standard errors toward an
empirical prior variance with prior degrees of freedom estimated from the
observed variance distribution. This maps to \code{testing = "moderated_t"} for
the model path; \code{FALSE} maps to \code{testing = "student_t"}.}

\item{empty_vector_reporter}{Optional reporter/control strain used as an
empty-vector reporter in the model-based path. When supplied, its
reference-relative perturbation effect is subtracted from every reporter's
reference-relative perturbation effect before reporter-library centering.
For new analyses, prefer \code{background_reporter} in \code{\link[=prepare_assay]{prepare_assay()}}, which
performs explicit response-level background calibration before model
fitting.}

\item{background_rank}{Non-negative integer. The default \code{0} removes no
latent background. Values \code{1} or \code{2} additionally subtract a low-rank
background term from the reference-relative total-effect matrix before
testing rank-adjusted total and reporter-specific residual effects. Use
\code{"auto"} to choose the rank by permutation parallel analysis.}

\item{background_rank_max}{Maximum rank considered when
\code{background_rank = "auto"}.}

\item{background_rank_permutations}{Number of permutations used when
\code{background_rank = "auto"}.}

\item{background_rank_threshold}{Permutation reference quantile used when
\code{background_rank = "auto"}.}

\item{background_rank_seed}{Optional random seed used when
\code{background_rank = "auto"}.}

\item{background_impute}{Method used to fill missing entries of the effect
matrix before singular-value decomposition. The default \code{"column_mean"}
replaces missing entries by the observed mean of the corresponding
perturbation column. \code{"global_mean"} and \code{"zero"} are also available.}

\item{normalization}{One of \code{"linear_model"}, \code{"median_polish"}, or
\code{"empty_vector"}. \code{"model"} and \code{"evc"} are accepted aliases.}

\item{testing}{One of \code{"student_t"}, \code{"moderated_t"}, or \code{"gaussian_z"}.}

\item{aggregation}{One of \code{"none"} or \code{"max_p"}.}

\item{adjustment}{One of \code{"global"}, \code{"by_reporter"}, or \code{"none"}.}

\item{interaction}{If \code{FALSE}, fit one Gaussian linear
model per reporter with the control perturbation as reference and the supplied
technical covariates as design terms. The latter is the scalable path for
reporter-specific perturbation effects. If \code{TRUE}, fit the historical full
reporter-by-perturbation interaction model.}

\item{preset}{Optional named preset: \code{"model"}, \code{"median_polish_legacy"}, or
\code{"empty_vector_control"}. Common aliases such as \code{"median_polish"} and
\code{"evc"} are accepted.}

\item{...}{For \code{normalization = "linear_model"} with a raw data frame,
arguments passed to \code{\link[=prepare_assay]{prepare_assay()}}, including \code{growth_exponent}. For
compatibility presets, arguments passed to the selected engine.}
}
\value{
A fitted DStressR object. The model path returns a \code{destress_fit};
compatibility presets return their corresponding legacy result objects.
}
\description{
\code{fit_destress()} is the main DStressR entry point. By default it fits the
model-based analysis, but it can also run named compatibility presets for the
legacy median-polish and Empty Vector Control analyses.
}
\details{
The staged options make the major statistical choices explicit:
normalization, test statistic and p-value calculation, replicate aggregation,
and p-value adjustment. Only implemented combinations are accepted. For the
model-based path, growth-response normalization is performed upstream by
\code{\link[=prepare_assay]{prepare_assay()}}, where \code{growth_exponent} can be fixed, estimated, or
supplied as reporter-specific values.
}
