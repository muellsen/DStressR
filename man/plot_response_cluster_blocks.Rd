% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_response_cluster_blocks}
\alias{plot_response_cluster_blocks}
\title{Clustered block map of a DStressR reporter-by-perturbation response matrix}
\usage{
plot_response_cluster_blocks(
  table,
  value = "specific_effect",
  reporter = "reporter",
  perturbation = "perturbation",
  perturbation_label = perturbation,
  show_perturbation_ids = TRUE,
  n_reporter_clusters = 6,
  n_perturbation_clusters = 14,
  missing_value = 0,
  clip_quantile = 0.98,
  show_counts = TRUE,
  title = "DStressR clustered response map",
  subtitle = NULL,
  xlab = "Perturbation clusters",
  ylab = "Reporter clusters",
  low = "#2166AC",
  mid = "white",
  high = "#B2182B"
)
}
\arguments{
\item{table}{A data frame with one row per reporter-perturbation pair.}

\item{value}{Numeric response/effect column to summarize.}

\item{reporter, perturbation}{Columns identifying reporters and perturbations.}

\item{perturbation_label}{Optional human-readable perturbation-name column. Defaults
to \code{perturbation}.}

\item{show_perturbation_ids}{If \code{TRUE}, append perturbation IDs in square brackets
to perturbation labels before clustering.}

\item{n_reporter_clusters, n_perturbation_clusters}{Number of dendrogram clusters
to use for reporters and perturbations.}

\item{missing_value}{Value used only for clustering missing matrix entries.
Block summaries are still computed from observed finite values.}

\item{clip_quantile}{Quantile of absolute block means used to clip the color
scale. Set to \code{1} to use the observed maximum.}

\item{show_counts}{If \code{TRUE}, annotate each tile with the number of perturbations
in that perturbation cluster.}

\item{title, subtitle, xlab, ylab}{Plot labels.}

\item{low, mid, high}{Colors for negative, zero, and positive responses.}
}
\value{
A \code{ggplot} object with attributes \code{response_matrix},
\code{reporter_clusters}, \code{perturbation_clusters}, \code{block_summary}, \code{row_hclust},
and \code{col_hclust}.
}
\description{
Hierarchically clusters reporters and perturbations, cuts the dendrograms into
interpretable groups, and plots the mean response for each reporter-cluster by
perturbation-cluster block. This is useful as a compact overview when the full
perturbation library is too large for individual perturbation labels.
}
