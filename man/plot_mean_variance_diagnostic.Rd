% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{plot_mean_variance_diagnostic}
\alias{plot_mean_variance_diagnostic}
\title{Plot perturbation-level mean-variance diagnostics}
\usage{
plot_mean_variance_diagnostic(
  table = NULL,
  diagnostics = NULL,
  mean_effect = "total_effect",
  variance_effect = mean_effect,
  reporter = "reporter",
  perturbation = "perturbation",
  perturbation_label = perturbation,
  min_reporters = 2,
  trend_span = 0.45,
  add_trend = TRUE,
  label_by = c("residual", "variance", "abs_mean", "none"),
  top_n = 8,
  point_size = 1.8,
  color_limits = NULL,
  color_quantile = 0.92,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  legend_position = c(0.035, 0.965),
  legend_justification = c(0, 1)
)
}
\arguments{
\item{table}{A data frame with one row per reporter-perturbation pair.}

\item{diagnostics}{Optional output from \code{\link[=perturbation_diagnostics]{perturbation_diagnostics()}}. If
supplied, \code{table} is ignored.}

\item{mean_effect}{Numeric column used to compute the perturbation-level mean
effect. For DStressR results, \code{total_effect} or
\code{rank_adjusted_total_effect} are typical choices.}

\item{variance_effect}{Numeric column whose cross-reporter variance is
computed for each perturbation. Defaults to \code{mean_effect}.}

\item{reporter, perturbation}{Columns identifying reporters and perturbations.}

\item{perturbation_label}{Optional human-readable perturbation label column.
Defaults to \code{perturbation}.}

\item{min_reporters}{Minimum number of finite reporter-level effects required
for a perturbation.}

\item{trend_span}{Span used for the loess trend of variance over
\verb{rank(abs(mean effect))}.}

\item{add_trend}{If \code{TRUE}, overlay the loess trend.}

\item{label_by}{Which perturbations to label. \code{"residual"} labels
perturbations with the largest variance residual above the trend,
\code{"variance"} labels the largest variances, \code{"abs_mean"} labels the largest
absolute mean effects, and \code{"none"} suppresses labels.}

\item{top_n}{Number of perturbations to label.}

\item{point_size}{Point size.}

\item{color_limits}{Optional numeric vector of length two for the color
scale. If omitted, symmetric robust limits are computed from
\code{color_quantile}.}

\item{color_quantile}{Quantile of the absolute mean effect used for robust
color limits when \code{color_limits} is omitted.}

\item{title, subtitle, xlab, ylab}{Plot labels.}

\item{legend_position, legend_justification}{Numeric vectors passed to
\code{ggplot2::theme()} to place the legend inside the plotting region.}
}
\value{
A \code{ggplot} object. The diagnostic table is available as
\code{attr(plot, "diagnostics")}.
}
\description{
Draws a DESeq-style diagnostic plot in which perturbations are ordered by
their absolute mean response and the y-axis shows the variance of
reporter-level effects. A non-parametric trend can be overlaid, and the most
heterogeneous perturbations can be labelled.
}
