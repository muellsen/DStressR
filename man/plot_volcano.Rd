% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot for DStressR promoter-compound hits}
\usage{
plot_volcano(
  table,
  effect = "specific_effect",
  padj = "specific_padj",
  pvalue = NULL,
  promoter = "promoter",
  compound = "compound",
  compound_label = compound,
  fdr = 0.05,
  lfc = 0,
  top_n = 12,
  top_promoters = 6,
  title = "DStressR volcano plot",
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  label_by = c("pair", "promoter", "compound"),
  max_label_chars = 46,
  repel_labels = TRUE,
  point_alpha = 0.65
)
}
\arguments{
\item{table}{A data frame with one row per promoter-compound pair.}

\item{effect}{Effect-size column to plot on the x-axis.}

\item{padj}{Adjusted p-value column to plot on the y-axis.}

\item{pvalue}{Optional raw p-value column used only if \code{padj = NULL}.}

\item{promoter, compound}{Columns identifying the promoter and compound.}

\item{compound_label}{Optional column with human-readable compound names used
for annotations. Defaults to \code{compound}.}

\item{fdr}{FDR threshold for hit highlighting.}

\item{lfc}{Minimum absolute effect size for hit highlighting.}

\item{top_n}{Number of significant pairs to annotate.}

\item{top_promoters}{Number of promoter groups to color. Remaining promoters
are shown in grey.}

\item{title, subtitle}{Plot title and subtitle.}

\item{xlab, ylab}{Axis labels. Defaults to readable labels based on the
selected columns.}

\item{label_by}{Label style for annotated points. The default, \code{"pair"},
labels top hits as promoter-compound pairs.}

\item{max_label_chars}{Maximum characters per annotation label. Longer labels
are truncated with \code{...}. Use \code{Inf} to keep full labels.}

\item{repel_labels}{If \code{TRUE} and the optional \code{ggrepel} package is
installed, use repelled labels for the annotated top hits.}

\item{point_alpha}{Point transparency.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a standard volcano plot from a DStressR result table. The x-axis is a
promoter-compound effect size and the y-axis is the negative log10 adjusted
p-value. Significant hits are emphasized, top promoter groups can be colored,
and the most significant promoter-compound pairs are annotated.
}
\details{
The defaults work with \code{\link[=results]{results()}} followed by
\code{\link[=adjust_pvalues]{adjust_pvalues()}} or
\code{\link[=call_hits]{call_hits()}}. For workflow comparison tables, pass the
corresponding column names, for example
\code{effect = "destress_eb_effect_centered"},
\code{padj = "estimated_alpha_eb_padj_by_promoter"},
\code{compound = "srn_code"}, and \code{compound_label = "ProductName"}.
}
