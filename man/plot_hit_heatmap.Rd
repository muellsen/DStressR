% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_hit_heatmap}
\alias{plot_hit_heatmap}
\title{Heatmap of significant DStressR hits}
\usage{
plot_hit_heatmap(
  table,
  effect = "specific_effect",
  padj = "specific_padj_by_reporter",
  reporter = "reporter",
  perturbation = "perturbation",
  perturbation_label = perturbation,
  show_perturbation_ids = TRUE,
  top_n_perturbations = 160,
  fdr = 0.05,
  lfc = 0,
  drop_empty_perturbations = TRUE,
  reporter_order = NULL,
  order_rows = c("global", "cluster", "input", "frequency"),
  order_cols = c("frequency", "cluster", "input"),
  clip_quantile = 0.98,
  color_limit = NULL,
  show_perturbation_labels = NULL,
  top_perturbation_labels = 40,
  perturbation_label_score = NULL,
  perturbation_label_min_gap = NULL,
  perturbation_label_angle = 45,
  title = "DStressR significant-hit matrix",
  subtitle = NULL,
  xlab = "Perturbations",
  ylab = "Reporters",
  legend_title = effect,
  low = "#2166AC",
  mid = "white",
  high = "#B2182B"
)
}
\arguments{
\item{table}{A data frame with one row per reporter-perturbation pair.}

\item{effect}{Effect-size column shown by color for significant hits.}

\item{padj}{Adjusted p-value column used for hit calls.}

\item{reporter, perturbation}{Columns identifying reporters and perturbations.}

\item{perturbation_label}{Optional human-readable perturbation-name column. Defaults
to \code{perturbation}.}

\item{show_perturbation_ids}{If \code{TRUE}, append perturbation IDs in square brackets
to perturbation labels.}

\item{top_n_perturbations}{If finite, show only perturbations with the strongest hit
evidence, ranked by hit count and effect size. Use \code{Inf} to show all
perturbations.}

\item{fdr}{FDR threshold for hit highlighting.}

\item{lfc}{Minimum absolute effect size for hit highlighting.}

\item{drop_empty_perturbations}{If \code{TRUE}, remove perturbations with no significant
hits from the displayed matrix.}

\item{reporter_order}{Optional global reporter order used when
\code{order_rows = "global"}. If omitted, the option \code{DStressR.reporter_order}
is used when set; otherwise known DStressR paper reporters are shown in
their manuscript order and remaining reporters are sorted alphabetically.}

\item{order_rows, order_cols}{Ordering strategy for reporters and perturbations.
Use \code{"global"} for the package-wide reporter order, \code{"input"} to preserve
the input/factor order, \code{"frequency"} to order by number of hits, or
\code{"cluster"} for hierarchical clustering of the hit matrix.}

\item{clip_quantile}{Quantile of absolute significant effects used to clip the
color scale. Set to \code{1} to use the observed maximum.}

\item{color_limit}{Optional positive color-scale limit. If supplied, values
are clipped to \verb{[-color_limit, color_limit]}; otherwise the limit is
computed from \code{clip_quantile}.}

\item{show_perturbation_labels}{If \code{TRUE}, draw all x-axis perturbation labels. If
\code{FALSE}, suppress x-axis perturbation labels. The default labels the
\code{top_perturbation_labels} perturbations with largest absolute column sums, or all
perturbations when fewer are plotted.}

\item{top_perturbation_labels}{Number of highest-signal perturbations to label when
\code{show_perturbation_labels = NULL}.}

\item{perturbation_label_score}{Optional numeric score used to choose the
top-labelled perturbations when \code{show_perturbation_labels = NULL}. If named, values
are matched to perturbation labels; otherwise the order must match the displayed
matrix columns.}

\item{perturbation_label_min_gap}{Minimum number of matrix columns between
automatically selected labels. The default chooses a gap from the displayed
matrix size and \code{top_perturbation_labels}.}

\item{perturbation_label_angle}{Angle used for visible perturbation labels.}

\item{title, subtitle, xlab, ylab}{Plot labels.}

\item{legend_title}{Colorbar title. Defaults to the selected \code{effect} column.}

\item{low, mid, high}{Colors for negative, zero, and positive hit effects.}
}
\value{
A \code{ggplot} object with attributes \code{hit_matrix}, \code{hit_summary},
\code{plotted_pairs}, and \code{color_limit}.
}
\description{
Shows a reporter-by-perturbation effect matrix with significant pairs highlighted
by color and non-significant pairs shown as a light background. This plot is a
compact companion to \code{\link[=plot_response_heatmap]{plot_response_heatmap()}} for inspecting the discovered
hit structure.
}
