% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_response_clustered_heatmap}
\alias{plot_response_clustered_heatmap}
\title{Clustered heatmap with reporter and perturbation dendrograms}
\usage{
plot_response_clustered_heatmap(
  table,
  value = "specific_effect",
  reporter = "reporter",
  perturbation = "perturbation",
  perturbation_label = perturbation,
  show_perturbation_ids = TRUE,
  top_n_perturbations = 400,
  n_reporter_clusters = 6,
  n_perturbation_clusters = 14,
  missing_value = 0,
  clip_quantile = 0.98,
  file = NULL,
  width = 14,
  height = 8,
  res = 300,
  title = "DStressR clustered response heatmap",
  subtitle = NULL,
  show_rownames = TRUE,
  show_colnames = FALSE,
  low = "#2166AC",
  mid = "white",
  high = "#B2182B"
)
}
\arguments{
\item{table}{A data frame with one row per reporter-perturbation pair.}

\item{value}{Numeric response/effect column to show in the heatmap.}

\item{reporter, perturbation}{Columns identifying reporters and perturbations.}

\item{perturbation_label}{Optional human-readable perturbation-name column. Defaults
to \code{perturbation}.}

\item{show_perturbation_ids}{If \code{TRUE}, append perturbation IDs in square brackets
to perturbation labels before clustering.}

\item{top_n_perturbations}{If finite, show only the top perturbations by mean
absolute response. Use \code{Inf} to show all perturbations.}

\item{n_reporter_clusters, n_perturbation_clusters}{Number of dendrogram clusters
returned in the cluster assignment tables.}

\item{missing_value}{Value used only for clustering missing matrix entries.
Heatmap cells with missing values are left missing.}

\item{clip_quantile}{Quantile of absolute response values used to clip the
color scale. Set to \code{1} to use the observed maximum.}

\item{file}{Optional output file. Supports \code{.png} and \code{.pdf}. If \code{NULL}, the
plot is drawn on the active graphics device.}

\item{width, height}{Plot size in inches when \code{file} is supplied.}

\item{res}{PNG resolution in dots per inch.}

\item{title, subtitle}{Plot title and subtitle.}

\item{show_rownames, show_colnames}{Whether to draw row and column labels.}

\item{low, mid, high}{Colors for negative, zero, and positive responses.}
}
\value{
Invisibly returns a list containing the response matrix, clustering
objects, ordered matrix, cluster assignments, and color limit.
}
\description{
Draws a clustered reporter-by-perturbation response heatmap with hierarchical
trees on both axes. Unlike \code{\link[=plot_response_cluster_blocks]{plot_response_cluster_blocks()}}, this keeps the
individual matrix cells visible and uses the dendrograms to reveal structure
without collapsing the data into coarse blocks.
}
