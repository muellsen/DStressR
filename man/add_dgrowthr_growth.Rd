% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgrowthr.R
\name{add_dgrowthr_growth}
\alias{add_dgrowthr_growth}
\title{Add DGrowthR-derived growth metrics to a DStressR assay table}
\usage{
add_dgrowthr_growth(
  data,
  object,
  by = "curve_id",
  model_covariate = by,
  growth_metric = "max_growth",
  output = "growth",
  keep_dgrowthr_columns = FALSE
)
}
\arguments{
\item{data}{A data frame with one row per DStressR assay observation.}

\item{object}{A DGrowthR object after \code{DGrowthR::estimate_growth_parameters()}.}

\item{by}{Column in \code{data} and \code{object@metadata} identifying the growth
curve for each assay row. DGrowthR calls this \code{curve_id}.}

\item{model_covariate}{Metadata column used as DGrowthR's
\code{estimate_growth_parameters(model_covariate = ...)}. Defaults to \code{by},
corresponding to one GP fit per growth curve.}

\item{growth_metric}{Column in \code{object@growth_parameters} to use as the
growth measurement. Common choices are \code{max_growth}, \code{AUC}, \code{OD_16}, and
\code{AUC_16}.}

\item{output}{Column name to create in \code{data}.}

\item{keep_dgrowthr_columns}{If \code{TRUE}, also keep the DGrowthR join key and
the unrenamed metric column when possible.}
}
\value{
\code{data} with an additional numeric growth column named by \code{output}.
}
\description{
DStressR is designed to work hand in hand with DGrowthR: DGrowthR models the
optical-density growth curves, and DStressR can use one chosen DGrowthR
growth metric as the growth covariate in \code{\link[=prepare_assay]{prepare_assay()}}.
}
\details{
This function is deliberately opt-in. The current hit-determination scripts
use the original exported growth summaries unless the analyst explicitly
calls this helper and passes the resulting column to \code{\link[=prepare_assay]{prepare_assay()}}. This
keeps the present analysis reproducible while making the DGrowthR bridge
available for future comparisons.

This helper joins \code{object@growth_parameters} from a fitted DGrowthR object
onto a promoter/luminescence assay table. Run
\code{DGrowthR::estimate_growth_parameters()} first, optionally with
\code{od_auc_at_t} if you want time-specific columns such as \code{OD_16} or \code{AUC_16}.
}
