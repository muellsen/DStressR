% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{fit_variance_distribution}
\alias{fit_variance_distribution}
\title{Fit diagnostic distributions to perturbation-level variances}
\usage{
fit_variance_distribution(
  diagnostics,
  variance = "effect_variance",
  distributions = c("beta_prime", "inverse_gamma", "log_normal", "log_t"),
  min_n = 10,
  gmm_starts = 50,
  gmm_min_weight = 0.08,
  gmm_sigma_floor = NULL,
  seed = 1
)
}
\arguments{
\item{diagnostics}{A data frame, usually from \code{\link[=perturbation_diagnostics]{perturbation_diagnostics()}}.}

\item{variance}{Numeric column containing positive variance estimates.}

\item{distributions}{Character vector of distributions to fit. Supported
values are \code{"beta_prime"}, \code{"inverse_gamma"}, \code{"log_normal"}, \code{"log_t"},
and \code{"two_gaussian"}.}

\item{min_n}{Minimum number of finite positive variances required.}

\item{gmm_starts}{Number of random starts for the two-Gaussian mixture.}

\item{gmm_min_weight}{Minimum component weight used during mixture fitting.}

\item{gmm_sigma_floor}{Optional lower bound for component standard deviations
on the log10 scale. If \code{NULL}, a data-adaptive floor is used.}

\item{seed}{Random seed used for mixture starting values.}
}
\value{
A data frame with one row per fitted distribution. The fitted
variance column is stored as the \code{"variance"} attribute.
}
\description{
Fits simple positive-support or log-scale distributions to the variance
column produced by \code{\link[=perturbation_diagnostics]{perturbation_diagnostics()}}. This is intended for
diagnostic use: it can help identify broad distributional structure in
perturbation-level heterogeneity, but it does not change DStressR inference.
}
\details{
The beta-prime and inverse-gamma distributions are fitted on the raw
variance scale and evaluated on the log10 scale. The log-normal and log-t
distributions are fitted directly to log10 variances. A two-Gaussian mixture
on log10 variances is available as an explicitly requested exploratory
option, but is not used by default.
}
