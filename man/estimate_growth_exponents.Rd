% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth.R
\name{estimate_growth_exponents}
\alias{estimate_growth_exponents}
\title{Estimate promoter-specific growth normalization exponents}
\usage{
estimate_growth_exponents(
  data,
  promoter = "promoter",
  compound = "compound",
  lux = "lux",
  growth = "growth",
  covariates = NULL,
  numeric_covariates = NULL,
  controls = "DMSO",
  pseudocount = 1e-08,
  min_control_n = 8,
  shrink = TRUE,
  alpha_bounds = c(-2, 3)
)
}
\arguments{
\item{data}{A data frame with one row per well.}

\item{promoter, compound, lux, growth}{Column names.}

\item{covariates}{Optional technical covariate column names to include as
additive adjustment terms when estimating growth slopes. Only covariates
with more than one observed value in the relevant control subset are used.}

\item{numeric_covariates}{Optional subset of \code{covariates} that should enter
the growth-slope model as numeric variables. Other covariates are converted
to factors.}

\item{controls}{Control values in \code{compound}, usually DMSO wells.}

\item{pseudocount}{Added before log2 transformation.}

\item{min_control_n}{Minimum control wells needed for a promoter-specific
raw slope. Promoters with fewer controls use the global slope.}

\item{shrink}{If \code{TRUE}, shrink promoter-specific slopes toward the global
control slope.}

\item{alpha_bounds}{Optional numeric length-2 bounds for the final exponent.
Use \code{NULL} for no clipping.}
}
\value{
A data frame with raw promoter intercepts and raw and shrunken growth
exponents per promoter.
}
\description{
Estimates how luminescence scales with growth in negative-control wells,
optionally adjusting for technical factors such as batch, plate, or
replicate:
}
\details{
\deqn{\log_2(LUX_i) = a_g + \alpha_g \log_2(growth_i) + X_i\theta + e_i}

Raw promoter-specific slopes are then shrunk toward a global control-well
slope using an empirical-Bayes normal prior. The shrunken
\eqn{\alpha_g} values can be used in \code{\link[=prepare_assay]{prepare_assay()}} to compute:

\deqn{y_i = \log_2(LUX_i) - \alpha_g \log_2(growth_i)}
}
