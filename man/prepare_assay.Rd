% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_assay}
\alias{prepare_assay}
\title{Prepare a chemical-genomics assay table}
\usage{
prepare_assay(
  data,
  reporter = "reporter",
  perturbation = "perturbation",
  control = "DMSO",
  lux = "lux",
  growth = "growth",
  growth_exponent = "estimate",
  control_values = control,
  response = NULL,
  batch = NULL,
  plate = NULL,
  replicate = NULL,
  growth_covariates = NULL,
  numeric_covariates = NULL,
  background_reporter = NULL,
  background_method = c("none", "subtract", "lm", "huber"),
  background_by = NULL,
  pseudocount = 1e-08
)
}
\arguments{
\item{data}{A data frame with one row per reporter-perturbation-replicate well.}

\item{reporter, perturbation}{Column names identifying reporter and perturbation.}

\item{control}{Label in \code{perturbation} for the negative control, usually DMSO.}

\item{lux, growth}{Column names for luminescence and growth summaries.}

\item{growth_exponent}{Fixed coefficient for growth normalization, a named
vector keyed by reporter, or \code{"estimate"} to estimate reporter-specific
exponents from controls.}

\item{control_values}{Values in \code{perturbation} used as controls for growth
exponent estimation. Defaults to \code{control}.}

\item{response}{Optional existing response column. If supplied, \code{lux} and
\code{growth} are not used to compute the response.}

\item{batch, plate, replicate}{Optional technical-factor column names.
When \code{growth_exponent = "estimate"}, these columns are also used as
covariates while estimating reporter-specific growth exponents unless
\code{growth_covariates} is supplied.}

\item{growth_covariates}{Optional technical covariate column names used only
while estimating reporter-specific growth exponents from control wells. If
\code{NULL}, DStressR uses the supplied \code{batch}, \code{plate}, and \code{replicate}
columns for backwards compatibility.}

\item{numeric_covariates}{Optional subset of technical covariate column names
that should remain numeric in model matrices. Other optional covariates are
converted to factors.}

\item{background_reporter}{Optional background reporter used as a background
reference, e.g. an Empty Vector Control. When supplied, the default
background method is \code{"huber"}. The background reporter is matched to other
reporters by \code{background_by}, the response is calibrated, and the
background reporter is excluded from model-based testing.}

\item{background_method}{One of \code{"none"}, \code{"subtract"}, \code{"lm"}, or
\code{"huber"}. If omitted, DStressR uses \code{"none"} when no
\code{background_reporter} is supplied and \code{"huber"} when one is supplied.
\code{"subtract"} subtracts the matched background response. \code{"lm"} and
\code{"huber"} replace each non-background reporter response by residuals from
a reporter-wise calibration against the matched background.}

\item{background_by}{Columns used to match each observation to the
background reporter. Defaults to \code{perturbation} plus the supplied technical
columns.}

\item{pseudocount}{Added before log2 transformation.}
}
\value{
A \code{destress_assay} data frame.
}
\description{
Computes a growth-adjusted log2 reporter-activity response from luminescence
and growth measurements. By default, reporter-specific growth exponents are
estimated from control wells with available technical-factor adjustment and
shrunk toward a global control-well slope.
Set \code{growth_exponent = 1} to reproduce the current workflow's log2(LUX / OD)
response.
}
